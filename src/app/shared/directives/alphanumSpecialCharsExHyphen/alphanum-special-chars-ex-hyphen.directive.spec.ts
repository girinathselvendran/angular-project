import { AlphanumSpecialCharsExHyphenDirective } from './alphanum-special-chars-ex-hyphen.directive';

describe('AlphanumSpecialCharsExHyphenDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphanumSpecialCharsExHyphenDirective();
    expect(directive).toBeTruthy();
  });
});
