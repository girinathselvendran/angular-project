import { TrimSpaceAtFirstDirective } from './trim-space-at-first.directive';

describe('TrimSpaceAtFirstDirective', () => {
  it('should create an instance', () => {
    const directive = new TrimSpaceAtFirstDirective();
    expect(directive).toBeTruthy();
  });
});
